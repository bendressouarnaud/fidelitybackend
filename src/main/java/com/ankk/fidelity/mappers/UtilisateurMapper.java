package com.ankk.fidelity.mappers;

import com.ankk.fidelity.httpbeans.UtilisateurBean;
import com.ankk.fidelity.model.Utilisateur;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;

//@Mapper(componentModel="spring")
public interface UtilisateurMapper {

    /*@Mapping(ignore = true, target = "id")
    @Mapping(ignore = true, target = "version")
    @Mapping(ignore = true, target = "creationDatetime")
    @Mapping(ignore = true, target = "lastUpdateDatetime")
    @Mapping(ignore = true, target = "calendrierPaiements")
    @Mapping(ignore = true, target = "points")
    Utilisateur toUser(UtilisateurBean data);*/
}
